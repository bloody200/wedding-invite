<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=yes, viewport-fit=cover">
  <title>Владимир & Карина | Свадьба 19 июля</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,400;0,500;0,600;1,400;1,500&family=Montserrat:wght@300;400;500&family=Marck+Script&family=Caveat:wght@400;700&display=swap" rel="stylesheet">
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Montserrat', sans-serif;
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      position: relative;
      overflow-x: hidden;
      overflow-y: auto;
      padding: 1.8rem;
      background-image: url('https://images.unsplash.com/photo-1519378058457-4c29a0a2efac?ixlib=rb-4.0.3&auto=format&fit=crop&w=1350&q=80');
      background-size: cover;
      background-position: center 35%;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }

    body::after {
      content: "";
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: linear-gradient(135deg, rgba(255, 240, 225, 0.35) 0%, rgba(255, 225, 200, 0.3) 50%, rgba(250, 235, 215, 0.4) 100%);
      z-index: 0;
      pointer-events: none;
      backdrop-filter: blur(2px);
      -webkit-backdrop-filter: blur(2px);
    }

    .feathers-background {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      z-index: 1;
      pointer-events: none;
      overflow: hidden;
    }

    .feather {
      position: absolute;
      top: -10%;
      opacity: 0.7;
      animation: fall linear infinite;
      will-change: transform;
      filter: drop-shadow(0 8px 18px rgba(180, 130, 90, 0.3));
    }

    .feather svg {
      width: 100%;
      height: 100%;
      display: block;
      fill: rgba(255, 240, 225, 0.85);
      transform: rotate(8deg);
    }

    @keyframes fall {
      0% {
        transform: translateY(-5vh) rotate(0deg) translateX(0px);
        opacity: 0.15;
      }
      10% {
        opacity: 0.7;
      }
      80% {
        opacity: 0.5;
      }
      100% {
        transform: translateY(105vh) rotate(370deg) translateX(55px);
        opacity: 0;
      }
    }

    @keyframes pulseHeart {
      0% { transform: scale(1); }
      15% { transform: scale(1.3); }
      30% { transform: scale(1); }
      45% { transform: scale(1.25); }
      60% { transform: scale(1); }
      100% { transform: scale(1); }
    }

    @keyframes glowPulse {
      0% { filter: drop-shadow(0 0 10px rgba(255, 150, 120, 0.7)); }
      50% { filter: drop-shadow(0 0 22px rgba(255, 130, 90, 0.95)); }
      100% { filter: drop-shadow(0 0 10px rgba(255, 150, 120, 0.7)); }
    }

    .pulsing-heart {
      display: inline-block;
      animation: pulseHeart 2.2s ease-in-out infinite, glowPulse 2.2s ease-in-out infinite;
      color: #f5907b;
      font-size: 3.4rem;
      line-height: 1;
      margin: 0 0.5rem;
      text-shadow: 0 0 20px rgba(255, 140, 100, 0.8);
      transform-origin: center center;
    }

    .music-button {
      position: fixed;
      bottom: 2rem;
      right: 2rem;
      z-index: 100;
      width: 60px;
      height: 60px;
      border-radius: 50%;
      background: rgba(255, 248, 238, 0.9);
      backdrop-filter: blur(16px);
      -webkit-backdrop-filter: blur(16px);
      border: 2px solid rgba(230, 180, 130, 0.6);
      box-shadow: 0 10px 25px rgba(180, 120, 70, 0.3);
      cursor: pointer;
      display: flex;
      align-items: center;
      justify-content: center;
      transition: all 0.3s ease;
      color: #c98b56;
      font-size: 1.8rem;
      user-select: none;
      -webkit-tap-highlight-color: transparent;
      outline: none;
    }

    .music-button:hover {
      transform: scale(1.08);
      box-shadow: 0 14px 30px rgba(190, 130, 80, 0.4);
      background: rgba(255, 250, 242, 0.95);
    }

    .music-button:active {
      transform: scale(0.95);
    }

    .music-icon {
      display: flex;
      align-items: center;
      justify-content: center;
      transition: all 0.3s ease;
    }

    .music-icon svg {
      width: 28px;
      height: 28px;
      fill: currentColor;
    }

    .music-button.playing {
      background: rgba(255, 240, 225, 0.95);
      border-color: rgba(240, 150, 110, 0.7);
      box-shadow: 0 10px 25px rgba(220, 140, 90, 0.4);
    }

    .music-button.playing .music-icon {
      animation: musicPulse 1.2s ease-in-out infinite;
    }

    @keyframes musicPulse {
      0%, 100% { transform: scale(1); }
      50% { transform: scale(1.12); }
    }

    .invitation-card {
      position: relative;
      z-index: 10;
      max-width: 850px;
      width: 100%;
      background: rgba(255, 248, 238, 0.82);
      backdrop-filter: blur(28px);
      -webkit-backdrop-filter: blur(28px);
      background: linear-gradient(160deg, rgba(255, 250, 242, 0.88) 0%, rgba(255, 240, 222, 0.9) 100%);
      border-radius: 3.8rem 3.8rem 3.2rem 3.2rem;
      padding: 3rem 2.5rem;
      box-shadow: 0 40px 60px rgba(180, 120, 70, 0.2), 0 15px 30px rgba(200, 140, 80, 0.2), inset 0 1px 0 rgba(255, 255, 255, 0.8);
      border: 1px solid rgba(240, 200, 160, 0.6);
      text-align: center;
      transition: all 0.4s ease;
      margin: 1rem 0;
    }

    .invitation-card::before {
      content: "";
      position: absolute;
      top: 14px;
      left: 14px;
      right: 14px;
      bottom: 14px;
      border: 1px dashed rgba(220, 180, 140, 0.45);
      border-radius: 3.3rem 3.3rem 2.8rem 2.8rem;
      pointer-events: none;
    }

    .decorative-line {
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 0.8rem;
      margin: 1.4rem 0;
      color: #e0b890;
      position: relative;
      z-index: 2;
    }

    .decorative-line span {
      font-size: 1.9rem;
      color: #dba775;
      text-shadow: 0 1px 6px rgba(220, 150, 100, 0.4);
    }

    .decorative-line::before,
    .decorative-line::after {
      content: "";
      height: 1px;
      background: linear-gradient(to right, transparent, #e6c199, transparent);
      flex: 1;
    }

    h1 {
      font-family: 'Marck Script', cursive;
      font-weight: 400;
      font-size: clamp(3.4rem, 9vw, 5.2rem);
      color: #6b4c38;
      letter-spacing: 1.5px;
      margin-bottom: 0.4rem;
      line-height: 1.2;
      text-shadow: 0 2px 8px rgba(200, 140, 100, 0.2);
      position: relative;
      z-index: 2;
      transform: rotate(-0.5deg);
    }

    .names-block {
      margin: 1.5rem 0 0.8rem;
      position: relative;
      z-index: 2;
    }

    .couple-names {
      font-family: 'Marck Script', cursive;
      font-weight: 400;
      font-size: clamp(2.9rem, 8vw, 4.4rem);
      color: #5a3e2e;
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 0.8rem;
      flex-wrap: wrap;
      letter-spacing: 0.9px;
      line-height: 1.3;
    }

    .date-box {
      background: rgba(255, 235, 210, 0.6);
      border-radius: 5rem;
      padding: 0.7rem 2.8rem;
      display: inline-block;
      margin: 1.8rem 0 1.2rem;
      backdrop-filter: blur(10px);
      border: 1px solid rgba(230, 180, 130, 0.55);
      position: relative;
      z-index: 2;
      box-shadow: 0 6px 18px rgba(200, 140, 90, 0.2);
    }

    .wedding-date {
      font-family: 'Marck Script', cursive;
      font-weight: 400;
      font-size: 2.1rem;
      letter-spacing: 0.2rem;
      color: #6b4832;
    }

    .info-grid {
      display: flex;
      flex-direction: column;
      gap: 2rem;
      margin: 2rem 0;
    }

    .info-card {
      background: rgba(255, 242, 225, 0.7);
      border-radius: 2.5rem;
      padding: 1.5rem 1.8rem;
      backdrop-filter: blur(14px);
      border: 1px solid rgba(230, 185, 135, 0.6);
      text-align: left;
      transition: all 0.3s ease;
    }

    .info-card:hover {
      background: rgba(255, 242, 225, 0.85);
      transform: translateY(-3px);
    }

    .info-title {
      font-family: 'Marck Script', cursive;
      font-size: 2.2rem;
      color: #5f3f2c;
      border-left: 4px solid #dba775;
      padding-left: 1rem;
      margin-bottom: 1.2rem;
      display: flex;
      align-items: center;
      gap: 0.8rem;
    }

    .info-title span {
      font-size: 1.8rem;
    }

    .photo-list {
      display: flex;
      flex-wrap: wrap;
      gap: 1rem;
      margin-top: 0.8rem;
    }

    .photo-item {
      background: rgba(255, 248, 238, 0.8);
      border-radius: 2rem;
      padding: 0.5rem 1.2rem;
      font-size: 1rem;
      color: #4a3022;
      border: 1px solid rgba(210, 160, 105, 0.4);
      display: inline-flex;
      align-items: center;
      gap: 0.5rem;
    }

    .photo-item::before {
      content: "📸";
      font-size: 1rem;
    }

    .dresscode-block {
      background: linear-gradient(135deg, rgba(255, 248, 240, 0.8), rgba(255, 240, 225, 0.8));
      border-radius: 2rem;
      padding: 1.2rem 1.5rem;
      margin-top: 0.8rem;
    }

    .dresscode-row {
      display: flex;
      align-items: center;
      gap: 1rem;
      margin: 0.8rem 0;
      flex-wrap: wrap;
    }

    .dresscode-icon {
      font-size: 1.8rem;
      min-width: 2.5rem;
    }

    .dresscode-text {
      font-size: 1rem;
      color: #4a3022;
      line-height: 1.4;
    }

    .dresscode-label {
      font-weight: 600;
      color: #6b4c38;
      margin-right: 0.5rem;
    }

    .color-swatch {
      display: inline-block;
      width: 20px;
      height: 20px;
      border-radius: 50%;
      margin: 0 0.2rem;
      vertical-align: middle;
      border: 1px solid rgba(0,0,0,0.1);
    }

    .swatch-white { background-color: #ffffff; box-shadow: inset 0 0 0 1px #ddd; }
    .swatch-black { background-color: #2c2c2c; }
    .swatch-beige { background-color: #f5e6d3; }

    .plan-section {
      background: rgba(255, 242, 225, 0.7);
      border-radius: 2.5rem;
      padding: 1.5rem 1.8rem;
      margin: 1rem 0;
      backdrop-filter: blur(14px);
      border: 1px solid rgba(230, 185, 135, 0.6);
      text-align: left;
    }

    .plan-title {
      font-family: 'Marck Script', cursive;
      font-weight: 400;
      font-size: 2.2rem;
      color: #5f3f2c;
      text-align: center;
      margin-bottom: 1.2rem;
      letter-spacing: 0.1rem;
    }

    .timeline {
      display: flex;
      flex-direction: column;
      gap: 1rem;
    }

    .timeline-item {
      display: flex;
      align-items: baseline;
      gap: 1rem;
      border-bottom: 1px dashed rgba(210, 165, 110, 0.5);
      padding-bottom: 0.6rem;
      flex-wrap: wrap;
    }

    .time {
      font-family: 'Montserrat', sans-serif;
      font-weight: 500;
      font-size: 0.95rem;
      background: rgba(235, 195, 140, 0.4);
      color: #3e2818;
      padding: 0.2rem 1rem;
      border-radius: 2rem;
      white-space: nowrap;
    }

    .event {
      font-family: 'Marck Script', cursive;
      font-weight: 400;
      font-size: 1.6rem;
      color: #4a3022;
      line-height: 1.3;
    }

    .location-section {
      margin: 1.5rem 0 1rem;
      background: rgba(255, 238, 218, 0.6);
      border-radius: 2.5rem;
      padding: 1.2rem 1.5rem;
      backdrop-filter: blur(12px);
      border: 1px solid rgba(225, 180, 130, 0.65);
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 0.3rem;
    }

    .location-icon {
      font-size: 2rem;
      color: #c98b56;
    }

    .location-label {
      font-family: 'Marck Script', cursive;
      font-weight: 400;
      font-size: 1.8rem;
      color: #5c3b26;
    }

    .location-address {
      font-family: 'Montserrat', sans-serif;
      font-weight: 400;
      font-size: 1rem;
      color: #4a301f;
      background: rgba(255, 248, 235, 0.8);
      padding: 0.4rem 1.5rem;
      border-radius: 2rem;
    }

    .footer-note {
      font-family: 'Marck Script', cursive;
      font-size: 2rem;
      color: #7d5a42;
      margin-top: 1.8rem;
      letter-spacing: 0.5px;
    }

    .heart-icon {
      font-size: 1.5rem;
      margin: 0 0.3rem;
      color: #e0926e;
    }

    .tap-hint {
      position: fixed;
      bottom: 6rem;
      right: 1.5rem;
      background: rgba(0,0,0,0.5);
      color: white;
      padding: 0.5rem 1rem;
      border-radius: 2rem;
      font-size: 0.8rem;
      z-index: 101;
      pointer-events: none;
      backdrop-filter: blur(4px);
      animation: fadeOut 3s ease-in-out forwards;
    }

    @keyframes fadeOut {
      0% { opacity: 1; }
      70% { opacity: 1; }
      100% { opacity: 0; visibility: hidden; }
    }

    @media (max-width: 550px) {
      .invitation-card {
        padding: 1.8rem 1.5rem;
      }
      .couple-names {
        font-size: 2.2rem;
        gap: 0.3rem;
      }
      .pulsing-heart {
        font-size: 2.5rem;
      }
      .event {
        font-size: 1.4rem;
      }
      .timeline-item {
        flex-direction: column;
        align-items: flex-start;
        gap: 0.3rem;
      }
      .info-title {
        font-size: 1.8rem;
      }
      .photo-item {
        font-size: 0.85rem;
        padding: 0.4rem 1rem;
      }
      .dresscode-text {
        font-size: 0.9rem;
      }
      .music-button {
        width: 52px;
        height: 52px;
        bottom: 1.5rem;
        right: 1.5rem;
      }
    }
  </style>
</head>
<body>
  <div class="feathers-background" id="feathersContainer"></div>

  <div class="tap-hint" id="tapHint">🎵 Нажмите в любом месте, чтобы включить музыку</div>

  <button class="music-button" id="musicToggle" aria-label="Включить музыку" title="Романтичная свадебная мелодия">
    <span class="music-icon" id="musicIcon">
      <svg viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
        <path d="M9 18V5l12-2v13"/>
        <circle cx="6" cy="18" r="3"/>
        <circle cx="18" cy="16" r="3"/>
      </svg>
    </span>
  </button>

  <main class="invitation-card">
    <div class="decorative-line"><span>✧</span></div>
    <h1>Свадьба</h1>
    <div class="names-block">
      <div class="couple-names">
        <span>Владимир</span>
        <span class="pulsing-heart">♥</span>
        <span>Карина</span>
      </div>
    </div>
    <div class="date-box">
      <div class="wedding-date">19 июля 2026</div>
    </div>

    <!-- НОВЫЙ БЛОК: ФОТОГРАФИИ И ДРЕСС-КОД -->
    <div class="info-grid">
      <!-- Подружки невесты -->
      <div class="info-card">
        <div class="info-title">
          <span>👰‍♀️</span> Для подружек невесты
        </div>
        <div class="photo-list">
          <div class="photo-item">Портрет с букетом</div>
          <div class="photo-item">Поправляем фату</div>
          <div class="photo-item">Смеёмся над тостом</div>
          <div class="photo-item">Помогаем с платьем</div>
          <div class="photo-item">Танец с невестой</div>
          <div class="photo-item">Бросаем букет (кадр)</div>
          <div class="photo-item">Обнимашки</div>
          <div class="photo-item">Пьём шампанское</div>
        </div>
      </div>

      <!-- Друзья жениха -->
      <div class="info-card">
        <div class="info-title">
          <span>🤵‍♂️</span> Для друзей жениха
        </div>
        <div class="photo-list">
          <div class="photo-item">Поправляем галстук</div>
          <div class="photo-item">Поднимаем бокал</div>
          <div class="photo-item">Красуемся с женихом</div>
          <div class="photo-item">Караоке-кадр</div>
          <div class="photo-item">Толкаем речь</div>
          <div class="photo-item">Выход с шампанским</div>
          <div class="photo-item">Крупный план улыбки</div>
          <div class="photo-item">Играем в подвижную игру</div>
        </div>
      </div>

      <!-- Дресс-код -->
      <div class="info-card">
        <div class="info-title">
          <span>👗</span> Дресс-код
        </div>
        <div class="dresscode-block">
          <div class="dresscode-row">
            <div class="dresscode-icon">👔</div>
            <div class="dresscode-text">
              <span class="dresscode-label">Мужчины:</span>
              белый верх <span class="color-swatch swatch-white"></span> + чёрный низ <span class="color-swatch swatch-black"></span>
              <span style="display:inline-block; margin-left:0.5rem;">(рубашка/пиджак белые, брюки чёрные)</span>
            </div>
          </div>
          <div class="dresscode-row">
            <div class="dresscode-icon">👗</div>
            <div class="dresscode-text">
              <span class="dresscode-label">Девушки:</span>
              бежевые тона <span class="color-swatch swatch-beige"></span> — от песочного до кофе с молоком
              <span style="display:inline-block; margin-left:0.5rem;">(платья, комбинезоны, костюмы)</span>
            </div>
          </div>
          <div class="dresscode-row" style="margin-top: 0.5rem;">
            <div class="dresscode-icon">💡</div>
            <div class="dresscode-text" style="font-size:0.9rem; opacity:0.8;">Будем рады видеть вас элегантными и красивыми ❤️</div>
          </div>
        </div>
      </div>
    </div>

    <div class="plan-section">
      <div class="plan-title">План дня</div>
      <div class="timeline">
        <div class="timeline-item"><span class="time">16:00</span><span class="event">Фуршет</span></div>
        <div class="timeline-item"><span class="time">16:30</span><span class="event">Регистрация</span></div>
        <div class="timeline-item"><span class="time">17:00</span><span class="event">Банкет</span></div>
        <div class="timeline-item"><span class="time">20:00</span><span class="event">Торт</span></div>
        <div class="timeline-item"><span class="time">22:00</span><span class="event">Финал</span></div>
      </div>
    </div>

    <div class="location-section">
      <div class="location-icon">📍</div>
      <div class="location-label">Место проведения</div>
      <div class="location-address">Залесье, Рязанское шоссе, 4А</div>
    </div>

    <div class="footer-note">
      С любовью, <span class="heart-icon">♥</span> Владимир и Карина
    </div>
    <div class="decorative-line"><span>✧</span></div>
  </main>

  <script>
    (function() {
      const musicButton = document.getElementById('musicToggle');
      const musicIcon = document.getElementById('musicIcon');
      const hint = document.getElementById('tapHint');
      
      const audio = new Audio();
      audio.loop = true;
      audio.volume = 0.55;
      audio.src = 'https://files.catbox.moe/ym1m91.mp3';
      
      let isPlaying = false;
      let playAttempted = false;
      
      function setPauseIcon() {
        musicIcon.innerHTML = `<svg viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><rect x="6" y="4" width="4" height="16" rx="1"/><rect x="14" y="4" width="4" height="16" rx="1"/></svg>`;
      }
      function setPlayIcon() {
        musicIcon.innerHTML = `<svg viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path d="M9 18V5l12-2v13"/><circle cx="6" cy="18" r="3"/><circle cx="18" cy="16" r="3"/></svg>`;
      }
      
      function playMusic() {
        if (isPlaying) return;
        audio.play().then(() => {
          musicButton.classList.add('playing');
          setPauseIcon();
          isPlaying = true;
          playAttempted = true;
          if (hint) hint.style.opacity = '0';
        }).catch(e => console.log('Нужно взаимодействие'));
      }
      function pauseMusic() {
        if (!isPlaying) return;
        audio.pause();
        musicButton.classList.remove('playing');
        setPlayIcon();
        isPlaying = false;
      }
      
      function startOnUserInteraction(e) {
        if (e.target.closest('#musicToggle')) return;
        if (!isPlaying && !playAttempted) playMusic();
      }
      document.body.addEventListener('click', startOnUserInteraction);
      document.body.addEventListener('touchstart', startOnUserInteraction, { passive: false });
      
      musicButton.addEventListener('click', (e) => {
        e.preventDefault();
        e.stopPropagation();
        isPlaying ? pauseMusic() : playMusic();
      });
      musicButton.addEventListener('touchstart', (e) => {
        e.preventDefault();
        e.stopPropagation();
        isPlaying ? pauseMusic() : playMusic();
      }, { passive: false });
      
      audio.addEventListener('ended', () => { if (isPlaying) { audio.currentTime = 0; audio.play().catch(()=>{}); } });
      audio.load();
      
      // Перья
      function createFeatherSVG() {
        return `<svg viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path d="M20.5 3.5C18 2 14 2.5 11 5C8.5 7 6.5 9.5 5.5 11.5C4.8 12.8 4 14.5 4 16C4 17.5 4.8 19 6 19.5C7 20 8 20 8.5 19C9 18 8.5 16.5 7.5 15.5C9 14 11.5 12 14 10C16.5 8 19.5 6.5 20.5 3.5Z" fill="currentColor" opacity="0.9"/></svg>`;
      }
      const container = document.getElementById('feathersContainer');
      if (container) {
        container.innerHTML = '';
        for (let i = 0; i < 24; i++) {
          const f = document.createElement('div');
          f.className = 'feather';
          const s = Math.floor(Math.random() * 30 + 26);
          f.style.width = s + 'px';
          f.style.height = s + 'px';
          f.style.left = (Math.random() * 94 + 2) + '%';
          f.style.animationDelay = (Math.random() * 10) + 's';
          f.style.animationDuration = (Math.random() * 12 + 10) + 's';
          f.innerHTML = createFeatherSVG();
          const svg = f.querySelector('svg');
          if (svg) svg.style.color = `hsla(${38 + Math.random() * 12}, ${55 + Math.random() * 25}%, ${75 + Math.random() * 20}%, ${0.5 + Math.random() * 0.4})`;
          container.appendChild(f);
        }
        setInterval(() => {
          const feathers = document.querySelectorAll('.feather');
          if (!feathers.length) return;
          const rand = feathers[Math.floor(Math.random() * feathers.length)];
          rand.style.left = (Math.random() * 94 + 2) + '%';
          rand.style.animation = 'none';
          rand.offsetHeight;
          rand.style.animation = `fall ${Math.random() * 11 + 9}s linear ${Math.random() * 3}s infinite`;
        }, 8500);
      }
    })();
  </script>
</body>
</html>
